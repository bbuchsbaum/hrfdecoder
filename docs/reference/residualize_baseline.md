# Residualize Y against a baseline model

Residualize Y against a baseline model

## Usage

``` r
residualize_baseline(Y, base_model = NULL)
```

## Arguments

- Y:

  numeric matrix (T x V)

- base_model:

  optional fmridesign::baseline_model
